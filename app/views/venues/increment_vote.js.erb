<% if @already_voted %>
	$("#notice").text("You have already voted for this event");

<% else %> {
	$("#venue_" + <%= @venue.id %> + " .voteCount").text(<%= @venue.votecount %>);
	voteSort();
	$("#notice").text("Your vote has been recorded");
}

<% end %>


